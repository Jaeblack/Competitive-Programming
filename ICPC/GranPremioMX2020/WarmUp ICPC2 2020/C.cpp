#include <bits/stdc++.h>

using namespace std;
int N, P,resp;
int main()
{
    ios_base::sync_with_stdio(0); cin.tie(0);
    cin >> N >> P;
    resp = (P*2) -N;
    cout << resp;
    return 0;
}
